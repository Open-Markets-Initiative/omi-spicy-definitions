module EurexT7EdciFbeV1Client;

import spicy;

%byte-order = spicy::ByteOrder::Little;

public type EurexT7EdciV1 = unit {
    client_message: ClientMessage[] &eod;
};

type ClientMessage = unit {
    message_header: MessageHeader;
};

type MessageHeader = unit {
    body_len: uint32;
    template_id: uint16;
};

