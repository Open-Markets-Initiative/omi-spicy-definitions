module EurexT7EdciFbeV1Server;

import spicy;

%byte-order = spicy::ByteOrder::Little;

public type EurexT7EdciV1 = unit {
    server_message: ServerMessage[] &eod;
};

type ServerMessage = unit {
    message_header: MessageHeader;
};

type MessageHeader = unit {
    body_len: uint32;
    template_id: uint16;
};

